{
  "name": "read_file",
  "description": "Reads the full text contents of a local file and returns them.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": { "type": "string", "description": "Absolute or relative path to the file to read." }
    },
    "required": ["path"]
  },
  "command": "./read",
  "path_params": ["path"]
}
